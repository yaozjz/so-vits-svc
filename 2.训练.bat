@echo off
chcp 65001
echo 注：模型默认每800步保存一次，只会保留最新的20个模型，请训练至试听满意即可，当有保存过模型下次开启即可从最后一次保存继续训练
echo ==========================================================================
echo.
echo ================================ 开始训练 ================================
.\env\python.exe train.py -c configs/config.json -m 44k
pause